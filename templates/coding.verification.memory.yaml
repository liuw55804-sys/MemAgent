# Copy this file into a local memory store, then replace placeholders.
# Suggested target: ~/.memagent/memories/<your-id>.memory.yaml

id: "mem_manual_verification_example"
created_at: "YYYY-MM-DDTHH:MM:SS+08:00"
domain: "coding"
kind: "verification"
topic: "<verification topic>"
scope:
  repo: "<repo-name-or-unknown>"
  module: "<module-or-unknown>"
triggers:
  - "<verification keyword>"
  - "<test keyword>"
applies_when:
  - "<when this verification flow should be used>"
recommended_action:
  type: "run_tool"
  target: "<test-or-verification-command>"
tool_recipes:
  - name: "<verification command>"
    command_template: "<command with placeholders>"
entrypoints: []
pitfalls:
  - "<what verification shortcut is misleading>"
verification:
  - "<step 1>"
  - "<step 2>"
  - "<step 3>"
next_time_prompt:
  - "<short instruction to give Codex next time>"
agents_md_suggestion:
  - "<whether this should become a stable AGENTS.md rule>"
sensitivity:
  level: "internal"
  exportable: false
status:
  maturity: "draft"
  promotion_candidate: false
source_note: |
  <where this memory came from>
