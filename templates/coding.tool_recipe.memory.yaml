# Copy this file into a local memory store, then replace placeholders.
# Suggested target: ~/.memagent/memories/<your-id>.memory.yaml

id: "mem_manual_tool_recipe_example"
created_at: "YYYY-MM-DDTHH:MM:SS+08:00"
domain: "coding"
kind: "tool_recipe"
topic: "<tool recipe topic>"
scope:
  repo: "<repo-name-or-unknown>"
  module: "<module-or-unknown>"
triggers:
  - "<tool-name>"
  - "<task keyword>"
applies_when:
  - "<when this command should be used>"
recommended_action:
  type: "run_tool"
  target: "<tool-name>"
tool_recipes:
  - name: "<recipe name>"
    command_template: "<command with placeholders>"
entrypoints: []
pitfalls:
  - "<common wrong command or slow path>"
verification:
  - "<how to verify the command output>"
next_time_prompt:
  - "<short instruction to give Codex next time>"
agents_md_suggestion:
  - "<whether this should become a stable AGENTS.md rule>"
sensitivity:
  level: "internal"
  exportable: false
status:
  maturity: "draft"
  promotion_candidate: false
source_note: |
  <where this memory came from>
